package sheet.sort.strategy;

public enum PieceSortStrategy {
    HEIGHT_DESC,
    WIDTH_DESC,
    LONGEST_SIDE_DESC,
    AREA_DESC,
    POINTS_TO_LONGEST_SIDE_RATIO_DESC,
    POINTS_TO_AREA_RATIO_DESC
}
